/*let array = ["India", "Srilanka"];
console.log(array);
array.push("Australia"); // push method: adding element to array at last
console.log(array);
array.pop(); // pop method: removes last element in array
console.log(array);
array.unshift("Zimbambwe"); // unshift method: adding element at beginning
console.log(array);
array.shift();   // shift method: to remove 1st element in array
console.log(array);*/

/*let arr = ["a", "z", "e", "b", "h"];
console.log(arr);
console.log(arr.sort()); // sort method: for re-arranging elements in alphabeticl order
console.log(arr.sort().reverse()); // reverse of sorted array
console.log(arr.join("")); // joins each element of array*/

// program which accepts input
/*input arr =[1,2,3,4,5,6]
final = 7
o/p = [[0,5],[1,4],[2,3]]    -- index pair*/

/*arr = [1, 2, 3, 4, 5, 6];
final = 7;
op = [];
for (let i = 0; i < arr.length; i++) {
  for (let j = 1; j < arr.length; j++) {
    if (arr[i] + arr[j] === final) {
      op.push([i, j]);
    }
  }
}
console.log("o/p is :", op);*/

// program to find input string is palindrome or not
/*let str = "hema";
final = str.split("").reverse().join("");
if (str === final) {
  console.log("it is palindrome");
} else {
  console.log("not palindrome");
}*/

let arr = ["david", 28, false];
console.log(arr);
